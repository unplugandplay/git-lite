#!/bin/bash
# git-lite wrapper script for macOS
# Clears gem environment to avoid conflicts

unset GEM_HOME
unset GEM_PATH
unset RUBYOPT
unset RUBYLIB

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec /usr/bin/env ruby "${SCRIPT_DIR}/git-lite.rb" "$@"
